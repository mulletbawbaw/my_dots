# Evita que apps (WebRTC, Discord, jogos) peçam buffers muito pequenos,
# o que causa xruns (cortes) em carga de CPU/GPU. 256/48000 ≈ 5,3 ms.
context.properties = {
  default.clock.min-quantum = 256
}
